import React from "react";


function Conditional() {
    return (
       // if input fruit 1 && input mood 1
            // return cocktail a
        // else if input fruit 2 && mood 1
            // return cocktail b
        // else if input fruit 3 && mood 1
            // return cocktail c
        // else if input fruit 1 && mood 2
            // return cocktail d
        // else if input fruit 1 && mood 2
            // return cocktail e
        // else if input fruit 1 && mood 2
            // return cocktail f
        // else if input fruit 1 && mood 3
            // return cocktail g
            <p>conditional placeholder</p>
    );
}

export default Conditional